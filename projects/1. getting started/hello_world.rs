// // The main funciton is special: It is always the first code that runs in every executable Rust program.
// // If there were parameters, then they would go into the ().
// // The function body is wrapped in {}.
fn main() {
    // Rust style is to indent with four spaces, not a tab! 

    // // println! calls a Rust macro. 
    println!("Hello, World!");
}

// // rustc [file name] compiles the code. After compiling successfully, rust outputs a binary executable.
// // 
